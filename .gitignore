node_modules/
dist/
dev-dist/
.DS_Store
*.tsbuildinfo
.env
.env.local
.env.*.local
# native build env (may hold internal key) — commit .env.capacitor.example instead
.env.capacitor

# Firebase/Google service config (contains API keys) — keep out of VCS
android/app/google-services.json
ios/App/App/GoogleService-Info.plist

# Android Studio / IntelliJ IDE files
android/.idea/

.claude
.husky

# local npm/npx -> pnpm proxy shims (tooling convenience, not project code)
.codex-bin/

# test ค้นหาร่ม 
/outputs/