<?xml version="1.0" encoding="utf-8"?>
<!--
  อนุญาต cleartext HTTP เฉพาะ host ของ dev backend (เทียบเท่า NSAllowsLocalNetworking บน iOS)
  - 10.0.2.2  = host machine จากในมุมมองของ Android Emulator (localhost ของ Mac)
  - localhost / 127.0.0.1 = กรณีรันบนเครื่องเดียวกัน
  production ที่ backend เป็น HTTPS ไม่ได้รับผลกระทบ — cleartext ปิดเป็น default ที่ host อื่น
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
